# Ghulam Ali — Ads Manager Website

Open `index.html` in a browser. The site is responsive and uses the supplied promotional design as the hero visual.

To publish:
1. Upload `index.html` and the `assets` folder to any static hosting service.
2. Replace the WhatsApp/email details if needed.
3. Connect your custom domain.

No framework or build step is required.
